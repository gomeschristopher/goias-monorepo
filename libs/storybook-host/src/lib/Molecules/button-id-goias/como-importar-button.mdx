import { Meta, Source, Canvas } from '@storybook/blocks';

import * as ButtonStories from './button-id-goias.component.stories';

<Meta of={ButtonStories} />

# Importando o componente Button 

Importe o botão da seguinte forma na sua aplicação Angular:

```ts
import { ButtonIdGoiasComponent } from '@goias-ssi/button-id-goias';
```

Utilize o seguinte código no HTML:

<Source />

E o botão deve ser renderizado dessa forma:

<Canvas of={ButtonStories.PrimaryButton} layout='centered' sourceState='none' />